#!/usr/bin/env bash
# Backup the /etc/hosts file before making changes
sudo cp /etc/hosts /etc/hosts.backup

# Modify localhost to resolve to 127.0.0.2
sudo sed -i 's/127.0.0.1 localhost/127.0.0.2 localhost/' /etc/hosts

# Add a new entry to resolve facebook.com to 8.8.8.8
echo "8.8.8.8 facebook.com" | sudo tee -a /etc/hosts > /dev/null

echo "Hostnames have been updated."
